// onClick={() => this.handleClick()}

// onClick={this.handleClick.bind(this)}


//handleClick = () => {

// This method uses lexical this binding which automatically binds them to the scope in which they are defined in.



// code
//}