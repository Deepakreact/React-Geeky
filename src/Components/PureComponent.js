// Prevent re-rendering if the previous state and props === current state and props

// there is no need for shouldComponentUpdate() Lifecycle Method

// Generally, In ReactJS, we use shouldComponentUpdate() =>decide whether the React component should re-render itself or  Not.

// decide whether the React component should re-render itself or  Not.